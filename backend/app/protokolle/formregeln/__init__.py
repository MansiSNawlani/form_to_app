"""The form rules: what is wrong with an answers document, and what is missing.

Every rule from features 4c to 9b, mirroring frontend/src/protokoll/regeln/
module for module. See regel.py for the contract they all share.
"""
