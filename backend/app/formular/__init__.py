"""The form definition: which fields the Protokoll has, and what they are called.

Read out of the extracted seed rather than typed again. Everything that needs to
know whether a field path is real goes through here, so there is one answer
rather than one per caller.
"""
